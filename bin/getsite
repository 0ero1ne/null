#!/usr/local/bin/bash
# Download website

wget \
     --recursive \
     --no-clobber \
     --page-requisites \
     --adjust-extension \
     --span-hosts \
     --convert-links \
     --domains $1 \
     --no-parent \
$1